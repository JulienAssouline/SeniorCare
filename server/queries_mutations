query {
  getCaregiver{
    id
    fullname
    location
    years_experience
    num_hired
    birthdate
    hourly_rate
    gender
    availability
    average_rating
  }
}

mutation {
  login(input: {
    email: "alam@alam.com",
    password: "12341234"
  }) {
    message
  }
}

# query {
#   getConversations {
#     id
#     caregiver_id
#     key_contact_id
#   }
# }


mutation{
  addMessages(content: "hi", conversation_id: 2) {
    message
  }
}

